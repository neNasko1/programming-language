let print = func(x: i64) void
let i64_to_bool = func(x: i64) bool
let is_zero = func(x: i64) bool

let sum = func (x: i64) i64 {
	if(is_zero(x)) {
		return 0;
	}

	return 1 + sum(x - 1);
}

let main = func() i64 {
	print(sum(1));

	return 0;
}

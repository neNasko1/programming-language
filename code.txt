let print = func(x: i64) void
let i64_to_bool = func(x: i64) bool
let is_zero = func(x: i64) bool

let get_ref = func(a: i64&) i64& {
	return a;
}

let swap = func(a: i64&, b: i64&) i64& {
	if(is_zero(a)) {
		return b;
	}

	return a;
}

let main = func() i64 {
	let a = 0;
	let b = 100;
	let c = swap(a, b);

	print(c);

	return 0;
}
